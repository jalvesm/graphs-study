- request file name
    # check if valid 
        - must be a string
        - must exist
    if not 
        type again

- request now the number of the desired vertex
    # checks:
        - must be an typeof int
        - must exist 
    if not,
        type again

- Each input must have a scape charactere (eg 0 or -1)
- perform DFS;
- classify divergent edges (eg: from ${vertex} to A; from ${vertex} to B;)
